function addDigit(digit) {
    const input = document.querySelector("input")

    input.value += digit
}

function clearInput() {
    const input = document.querySelector("input")

    input.value = ''
}